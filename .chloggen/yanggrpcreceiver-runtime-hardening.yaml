change_type: enhancement
component: receiver/yang_grpc
note: Harden dial-out stream processing and publish runtime-hardening contract v1.
issues: [42647]
subtext: |
  Adds bounded connection counts and lifetimes, receiver-wide conversion concurrency, per-payload conversion ceilings, stream-scoped downstream contexts, per-message security enforcement, cleanup that cannot reset rate-limit tokens early, deadline-aware shutdown, and the RuntimeHardeningVersion contract used by embedding receivers.
change_logs: [api, user]

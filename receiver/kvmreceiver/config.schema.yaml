type: object
properties:
  endpoint:
    description: Endpoint is the libvirt URI used to connect to the QEMU/KVM daemon. The default is qemu:///system. Remote transports such as qemu+ssh and qemu+tls are also supported by the libvirt client.
    type: string
allOf:
  - $ref: go.opentelemetry.io/collector/scraper/scraperhelper.controller_config
  - $ref: ./internal/metadata.metrics_builder_config
